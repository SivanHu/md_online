interface:
  display_name: "Upload Markdown"
  short_description: "Upload an existing Markdown file to Sivan Note"
  default_prompt: "Use $upload-markdown to upload this Markdown file to Sivan Note without changing its content."
